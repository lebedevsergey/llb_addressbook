<form style="display:inline" action="{{ url('entry_delete', {id: item.id}) }}" method="post" data-confirmation="true" id="delete-form">
    <input type="hidden" name="token" value="{{ csrf_token('delete') }}" />
    <button type="submit" class="btn btn-sm btn-danger">
        <i class="fa fa-trash" aria-hidden="true"></i>
        {{ 'action.delete_entry'|trans }}
    </button>
</form>
{{ include('/addressee/item/_delete_confirmation.html.twig') }}